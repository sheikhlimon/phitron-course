#include <stdio.h>
#include <string.h>

int main()
{
    int n;
    scanf("%d", n);
    int a[n];
    ptr
    int prod = 1;
    for (int i = 0; i < n; i++)
    {
        prod = prod * *a+i;
    }
    printf("%d", prod);

    return 0;
}